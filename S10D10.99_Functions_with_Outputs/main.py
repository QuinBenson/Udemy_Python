def format_name(f_name, l_name):
    #simple fn to ucase leading letters
    #and lcase the rest of the word

    # pass #null op
  
    # returns, in this case, a tuple: a static? list. 
    return f"{f_name} {l_name}".title(), "BING"

print(format_name("FREDEGAR", "bolger"))
